{
  "name": "hearcare-pro",
  "private": true,
  "workspaces": ["web", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "concurrently \"npm run start -w server\" \"npm run start -w web\"",
    "android:sync": "npm run build -w web && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  }
}
